# editorconfig

这是我通用的 editorconfig 文件

import CodeBlock from '@theme/CodeBlock'
import EditorConfig from '!!raw-loader!@site/.editorconfig'

<CodeBlock language="markdown">{EditorConfig}</CodeBlock>
